import React, { useMemo, useRef, useState } from 'react';
import { Input, Tabs, Collapse, Empty } from 'antd';
import { SearchOutlined } from '@ant-design/icons';

import './index.scss';

export default ({  }) => {

    return (
        <div className="variable-config"></div>
    )
}